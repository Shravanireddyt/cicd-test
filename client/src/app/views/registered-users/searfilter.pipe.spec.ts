import { SearfilterPipe } from './searfilter.pipe';

describe('SearfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
